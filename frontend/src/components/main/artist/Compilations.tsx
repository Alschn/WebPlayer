import React, {FC} from "react";

const Compilations: FC<{ artistId: string }> = ({artistId}) => {
  return (
    <div>
      Artist in compilation playlist
    </div>
  );
};

export default Compilations;
