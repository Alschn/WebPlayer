import React, {FC} from "react";

const RelatedArtists: FC<{ artistId: string }> = ({artistId}) => {
  return (
    <div>
      Artists related to them
    </div>
  );
};

export default RelatedArtists;
